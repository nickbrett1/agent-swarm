import { performance } from 'perf_hooks';
import { PuppeteerBrowserHelper } from './dist/browser.js';
import puppeteer from '@cloudflare/puppeteer';

// To mock puppeteer, we would need to run vitest
